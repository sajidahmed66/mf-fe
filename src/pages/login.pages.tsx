import LoginComponent from "@/components/login/login";

const Login = () => {
  return (
    <div
      style={{
        width: "100%",
      }}
    >
      <LoginComponent />
    </div>
  );
};

export default Login;
